You are acting as a World-Class Python Software Architect. Your mission is to thoroughly audit, restructure, and refactor the following Python codebase. 

We are strictly rejecting heavy, bureaucratic Java-centric patterns (e.g., Spring-like IoC containers, bloated Service/DAO classes, nested DTO/VO/PO mappings, and manual boilerplate copying). Instead, we enforce a Modern Pythonic Clean Architecture that leverages the dynamic power, speed, and elegance of Python 3.10+.

### 🎯 Core Architectural Mandates (Non-Negotiable)

1. **The Role of dataclasses (Rich Domain Models without Side-Effects)**
   - Use `dataclass` (or Pydantic v2 where input validation/parsing at boundaries is required) to represent core domain concepts and entities.
   - **Allowed in dataclass:** Data attributes, type hints, `__post_init__` for domain invariant validation, `@property` decorators for pure in-memory calculations, and state-transition methods (e.g., `def mark_as_paid(self)`).
   - **Strictly Banned in dataclass:** Network I/O, database access (SQLAlchemy, Motor, etc.), logging frameworks, or direct importing of external infrastructure adapters.

2. **The "Rich Module + Pure Functions" Pattern (Application Layer)**
   - Do not wrap pure orchestration logic inside arbitrary `ClassService` constructs. In Python, modules (files) are first-class namespaces, and functions are first-class citizens.
   - Orchestration, business pipelines, and use-cases must be high-cohesion, top-level functions within their respective domain modules.

3. **Explicit Dependency Injection via Duck Typing & Protocols**
   - External dependencies (e.g., payment gateways, notification senders) must be passed into domain methods or use-case functions explicitly as parameters.
   - Use `typing.Protocol` to define structural subtyping (duck typing) for these external adapters, keeping the core domain entirely decoupled from concrete implementations.

4. **Pythonic Boundary Control via Context Managers**
   - Use `with` statements and `contextlib.contextmanager` to govern transactional boundaries, database sessions, and resource lifecycles cleanly without injecting heavy transaction managers.

5. **Advanced Control Flow via Pattern Matching**
   - Replace deep, nested `if-elif-else` blocks or overly complex GoF Strategy patterns with Python 3.10+ structural pattern matching (`match-case`) to handle complex polymorphism or type-based business rules cleanly.

6. **Immutability and State Safety**
   - For pure value objects, domain events, or cross-layer data transfers, enforce `@dataclass(frozen=True)`. Leverage `dataclasses.replace()` to return mutated copies when capturing new state snapshots.

---

### 🛠️ Execution Plan for Claude Code

1. **Audit & Diagnose:** Analyze the provided codebase. Identify violations where business/infrastructure logic leaks into `dataclass` objects or where unnecessary Java-like abstractions are cluttering the Python workflow.
2. **Modular Restructuring:** Group code into logical Python modules (e.g., `domain`, `adapters`, `entrypoints`/`usecases`) following a lean, decoupled schema.
3. **Refactor & Deliver:** Rewrite the code completely. Ensure strict type-hinting (compatible with MyPy/Pyright), use proper idiomatic error handling (domain-specific exceptions), and make sure the code is highly testable with standard `pytest` tools without complex deep-mocking hacks.

---

### 📂 Input Codebase to Refactor:

/Users/nako/PycharmProjects/github/thefoxfairy/PythonProject/Aura/aura